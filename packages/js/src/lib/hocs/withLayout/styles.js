import { createStyles } from "@material-ui/core";

const styles = () =>
  createStyles({
    section: {
      paddingTop: "50px",
      display: "flex"
    },
    component: {
      flexGrow: 1,
      padding: "30px 15px 0 15px"
    }
  });

export default styles;
